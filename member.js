/* 面试练场 PRO：在普通版基础上追加会员专属数据、智能索引和全权限展示。 */

materials.push(
  { id:7, type:"人物典例", title:"重庆山火中的摩托车志愿者：凡人微光汇成救援力量", summary:"山火救援中，众多普通市民主动运送物资、保障通道，用实际行动回应公共需要。", tags:["乐于助人","志愿服务","青年担当","重庆"], body:"面对突发山火，摩托车骑手、社区居民和社会志愿者自发参与物资转运、道路引导和后勤保障。个体力量通过组织协同形成了强大的社会互助网络。", use:"可用于乐于助人、社会责任、青年担当、基层应急和社会动员等题目。答题时既要赞扬善意，也要强调专业救援、统一调度和安全边界。" },
  { id:8, type:"改革案例", title:"浙江“最多跑一次”：以流程再造回应群众办事痛点", summary:"从部门视角转向群众视角，通过数据共享和事项整合改善政务体验。", tags:["浙江","数字政府","为民服务","营商环境"], body:"改革围绕群众和企业办事需求，推动事项标准化、材料精简、数据共享和跨部门协同，让更多事项从多头办理转向一次提交、协同完成。", use:"适合数字政府、服务型政府、改革创新、营商环境等主题。建议使用“需求导向—流程再造—数据支撑—风险兜底”的逻辑。" },
  { id:9, type:"精神谱系", title:"塞罕坝建设者：在平凡岗位上创造绿色奇迹", summary:"几代建设者长期坚守艰苦地区，以专业和韧性推动生态修复。", tags:["爱岗敬业","担当作为","生态文明","河北"], body:"塞罕坝建设者长期面对高寒、风沙等困难，坚持植树造林、技术改进和生态管护，把荒原逐步建设为重要生态屏障。", use:"适用于爱岗敬业、艰苦奋斗、生态文明、长期主义和青年择业等主题。不要只讲奉献，还可补充科学方法与制度接力。" },
  { id:10, type:"人物典例", title:"樊锦诗守护敦煌：把个人选择融入文化传承", summary:"长期扎根文物保护一线，以专业研究和数字技术延续文化生命。", tags:["爱岗敬业","文化传承","担当作为","科技赋能"], body:"长期的文物保护工作需要耐心、专业和跨代接力。通过数字化采集、科学保护与公众传播，文化遗产获得更稳定的保存和更广泛的理解。", use:"可用于文化自信、敬业奉献、科技与文化融合、青年价值选择等题目。" },
  { id:11, type:"科技案例", title:"城市治理引入AI助手：效率提升之后仍需责任闭环", summary:"智能识别和辅助决策可以提高响应速度，但不能替代人的责任判断。", tags:["广东","人工智能","科技治理","风险意识"], body:"部分城市探索用智能系统辅助热线分派、事件识别和公共服务，但算法偏差、隐私保护、特殊群体使用和责任追溯仍需制度保障。", use:"适用于人工智能、智慧城市和技术治理题。可使用“效率价值—潜在风险—制度边界—人工兜底”的结构。" },
  { id:12, type:"基层实践", title:"社区微网格：让治理触角抵达居民身边", summary:"通过细化责任单元、常态走访和问题台账，提高社区问题发现与响应能力。", tags:["四川","基层治理","社区治理","群众工作"], body:"社区将服务和治理责任细化到更小单元，由社区工作者、网格力量和志愿者共同发现诉求、分类处置并反馈结果。", use:"适用于基层治理、社区服务、矛盾排查和应急响应。答题中要防止网格形式化和基层负担过重。" },
  { id:13, type:"公共服务", title:"暖心窗口延时服务：便利之外更要形成长效机制", summary:"工作人员主动帮助群众值得肯定，更应通过预约、帮办和弹性服务形成制度。", tags:["乐于助人","为民服务","窗口服务","制度建设"], body:"群众临近下班仍有紧急事项，窗口人员主动提供延时和帮办服务，解决了现实困难。个体善意需要通过服务标准、轮班安排和线上渠道转化为稳定机制。", use:"适用于服务意识、职业道德、好人好事和制度建设。可采用“肯定善意—分析需求—制度固化”的表达。" },
  { id:14, type:"团队案例", title:"跨部门联合专班：复杂任务中的协同不是简单相加", summary:"明确目标、责任清单和信息接口，才能避免多部门协作中的推诿与重复。", tags:["团队合作","组织协调","担当作为","执行力"], body:"面对涉及多个部门的复杂任务，联合专班通过任务分解、定期会商、数据共享和责任追踪形成工作闭环。", use:"适用于组织管理、人际协作、重大活动和专项整治题目。" }
);

questions.push(
  { id:107, year:"2025模拟", region:"浙江专题", type:"综合分析", title:"某地持续推进政务服务“一件事一次办”，但部分老年人仍反映线上操作困难。对此你怎么看？", summary:"考查改革成效、数字适老化、线上线下协同与服务公平。", tags:["浙江","数字政府","为民服务","营商环境"] },
  { id:108, year:"2025模拟", region:"河北专题", type:"综合分析", title:"有人认为，新时代条件改善了，塞罕坝式的艰苦奋斗精神已经过时。请谈谈你的看法。", summary:"考查精神价值的时代转化、敬业担当与科学实干。", tags:["爱岗敬业","担当作为","生态文明","河北"] },
  { id:109, year:"2024模拟", region:"重庆专题", type:"组织管理", title:"单位计划开展“凡人善举·社区互助”志愿服务月活动，由你负责，你如何组织？", summary:"考查需求调研、志愿者管理、风险保障和长效机制。", tags:["乐于助人","志愿服务","组织管理","重庆"] },
  { id:110, year:"2025模拟", region:"广东专题", type:"综合分析", title:"城市治理中使用AI识别群众诉求，有人认为效率更高，也有人担心算法替代人工判断。你怎么看？", summary:"考查科技治理、隐私公平、责任边界与人工兜底。", tags:["广东","人工智能","科技治理","风险意识"] },
  { id:111, year:"2024模拟", region:"四川专题", type:"应急应变", title:"暴雨后社区多处积水，网格群信息混乱，独居老人求助，作为社区负责人你怎么办？", summary:"考查信息核实、重点人群保障、资源调度和灾后复盘。", tags:["四川","基层治理","应急处置","群众工作"] },
  { id:112, year:"2025模拟", region:"国考专题", type:"人际关系", title:"你负责一项跨部门任务，部分同事认为本部门工作最重，不愿继续配合，你怎么办？", summary:"考查目标共识、责任协调、换位沟通与工作闭环。", tags:["团队合作","组织协调","担当作为","执行力"] }
);

const memberIndexGroups = [
  { name:"品德品质", key:"德", tags:["爱岗敬业","担当作为","乐于助人","为民服务","团队合作"] },
  { name:"地区专题", key:"地", tags:["浙江","广东","四川","重庆","河北","国考"] },
  { name:"领域热点", key:"域", tags:["基层治理","数字政府","人工智能","文化传承","生态文明","营商环境"] },
  { name:"能力题型", key:"题", tags:["综合分析","组织管理","应急处置","群众工作","组织协调"] }
];

const memberAngles = {
  "爱岗敬业":"价值认同 → 长期坚守 → 专业能力 → 平凡岗位创造公共价值",
  "担当作为":"明确责任 → 主动破题 → 协同推进 → 结果反馈与复盘",
  "乐于助人":"肯定善意 → 分析社会价值 → 强调专业边界 → 推动互助机制化",
  "为民服务":"群众需求 → 服务堵点 → 流程改进 → 公平可及与满意度",
  "浙江":"改革意识 → 数字赋能 → 群众体验 → 基层承接与适老兜底",
  "人工智能":"效率价值 → 算法风险 → 数据安全 → 人工兜底和责任追溯",
  "基层治理":"需求发现 → 多元协商 → 资源下沉 → 闭环反馈和减负增效",
  "文化传承":"文化价值 → 现实困境 → 创新表达 → 专业保护与人才接续",
  "组织管理":"明确目标 → 调研对象 → 流程分工 → 风险预案 → 效果复盘",
  "应急处置":"控制现场 → 重点保障 → 信息发布 → 资源调度 → 复盘整改"
};

function memberMatchesTag(item, tag) {
  return item.tags.includes(tag) || item.type === tag || item.region?.includes(tag);
}

function memberResultCounts(tag) {
  return {
    questions: questions.filter(item => memberMatchesTag(item, tag)).length,
    materials: materials.filter(item => memberMatchesTag(item, tag)).length
  };
}

function insertMemberInterface() {
  document.querySelector('.topbar>div').insertAdjacentHTML('beforeend', '<span class="member-top-badge">PRO 全功能会员</span>');
  document.querySelector('.eyebrow').textContent = '公务员面试训练场 · MEMBER EDITION';
  document.querySelector('.status-pill').innerHTML = '<i></i> PRO 已解锁 · AI与排位不限次';
  document.querySelector('.profile-card .member-tag').textContent = 'PRO会员 · 2027.07到期';

  const homeProfile = document.querySelector('.home-profile-card');
  homeProfile.insertAdjacentHTML('afterend', `
    <article class="member-status-card">
      <div class="member-crown">PRO</div>
      <div><span>全部权益已生效</span><strong>AI训练、真人排位、智能索引、详细报告不限使用</strong><small>本月已节省预计 ¥286 · 连续会员 8 个月</small></div>
      <button data-nav="library">使用智能索引 ›</button>
    </article>`);
  document.querySelector('.member-status-card [data-nav="library"]').addEventListener('click', () => navigate('library'));

  const smartCard = document.querySelector('.smart-search-card');
  smartCard.querySelector('span').textContent = 'PRO 智能检索已开启';
  smartCard.querySelector('strong').textContent = '点击任意考点标签，关联真题、典例、答题角度与可视化模板';
  const unlockButton = smartCard.querySelector('button');
  unlockButton.removeAttribute('data-open-sheet');
  unlockButton.textContent = '已解锁 ✓';
  unlockButton.addEventListener('click', () => toast('PRO会员智能检索已处于开启状态'));

  document.querySelector('#ai-sheet .match-note').innerHTML = '<span>PRO会员训练权益</span><strong>不限次数 · 完整报告</strong>';
  document.querySelector('#match-sheet .match-note').insertAdjacentHTML('beforebegin', '<div class="member-unlimited-note">PRO会员：排位赛不限每日场次，优先匹配同段位活跃用户</div>');

  const reportLock = document.querySelector('.member-report-lock');
  reportLock.className = 'member-detailed-report';
  reportLock.innerHTML = `
    <div class="member-detail-head"><div><span>PRO 深度诊断</span><h3>近7次表达短板与训练建议</h3></div><em>已解锁</em></div>
    <div class="member-detail-grid">
      <article><span>口头禅</span><strong>“然后” 11次</strong><small>较上周下降 28%</small></article>
      <article><span>最长停顿</span><strong>3.2秒</strong><small>发生在对策转换处</small></article>
      <article><span>结构完整度</span><strong>86%</strong><small>结尾升华偏重复</small></article>
      <article><span>建议专项</span><strong>组织管理</strong><small>未来7天练习 5 题</small></article>
    </div>
    <button data-demo-action="已生成未来7天PRO训练计划">生成专项训练计划</button>`;

  const templateQuota = document.querySelector('.template-quota');
  templateQuota.innerHTML = '<div><span>PRO会员</span><strong>已创建 4 套 · 数量不限</strong><small>支持老师分享、圈子导入和答题时悬浮提示</small></div><button data-demo-action="已打开新模板编辑器">＋ 新建模板</button>';

  const memberMenuButton = document.querySelector("#page-profile button[data-open-sheet='membership-sheet']");
  memberMenuButton.querySelector('strong').textContent = 'PRO会员中心与充值';
  memberMenuButton.querySelector('small').textContent = '有效期至 2027.07 · 全权益已开启';

  const memberPanel = document.querySelector("[data-commerce-panel='member']");
  memberPanel.innerHTML = `
    <div class="member-active-plan"><span>当前套餐</span><strong>练场 PRO 年度会员</strong><small>有效期至 2027年7月17日 · 已连续开通8个月</small><em>生效中</em></div>
    <div class="benefit-list"><p>✓ AI训练、三人排位与好友房不限次数</p><p>✓ 多维标签智能索引与素材关联图谱</p><p>✓ 表达细节、口头禅及专项计划完整报告</p><p>✓ 可视化答题模板不限套数</p><p>✓ 一对一指导会员价与优先预约</p></div>
    <div class="member-renew-row"><button data-demo-action="续费功能将在正式版接入">续费一年 ¥888</button><button data-demo-action="会员兑换码入口已打开">兑换会员</button></div>`;

  document.querySelector('.detailed-coach-list article:first-child>em').textContent = '会员价 ¥359/次';
  document.querySelector('.detailed-coach-list article:nth-child(2)>em').textContent = '会员价 ¥149/次';
}

function insertMemberIndex() {
  const groupHtml = memberIndexGroups.map(group => `
    <article class="member-index-group">
      <div class="member-index-group-title"><i>${group.key}</i><div><span>${group.name}</span><strong>${group.tags.length} 个高频索引</strong></div></div>
      <div class="member-index-tags">${group.tags.map(tag => {
        const counts = memberResultCounts(tag);
        return `<button data-member-index="${tag}"><span>${tag}</span><small>${counts.questions}题 · ${counts.materials}素材</small></button>`;
      }).join('')}</div>
    </article>`).join('');

  document.querySelector('.smart-search-card').insertAdjacentHTML('afterend', `
    <section class="member-index-panel">
      <div class="member-index-heading"><div><span>PRO KNOWLEDGE GRAPH</span><h2>真题与素材智能索引</h2><p>从一个考点出发，同时找到可练真题、可讲典例、答题逻辑和模板提示。</p></div><em>全库已解锁</em></div>
      <div class="member-index-groups">${groupHtml}</div>
      <article class="member-index-example" id="member-index-example">
        <span>索引示例</span><strong>点击“基层治理”</strong><p>系统将串联：历年真题 → 地方实践 → 群众工作素材 → 对策表达模板</p>
      </article>
    </section>`);

  document.body.insertAdjacentHTML('beforeend', `
    <section class="bottom-sheet member-index-sheet" id="member-index-sheet" aria-modal="true" role="dialog">
      <button class="sheet-close" data-member-close aria-label="关闭">×</button>
      <div id="member-index-detail"></div>
    </section>`);

  document.querySelector('[data-member-close]').addEventListener('click', closeSheets);
  document.querySelectorAll('[data-member-index]').forEach(button => button.addEventListener('click', () => showMemberIndex(button.dataset.memberIndex)));
}

function showMemberIndex(tag) {
  const relatedQuestions = questions.filter(item => memberMatchesTag(item, tag)).slice(0, 3);
  const relatedMaterials = materials.filter(item => memberMatchesTag(item, tag)).slice(0, 3);
  const angle = memberAngles[tag] || `${tag}的价值定位 → 现实问题 → 原因影响 → 可执行对策 → 长效机制`;
  document.querySelector('#member-index-example').innerHTML = `<span>当前智能索引</span><strong>${tag}</strong><p>已关联 ${relatedQuestions.length} 道精选题、${relatedMaterials.length} 个典例和 1 套答题逻辑。</p>`;
  document.querySelector('#member-index-detail').innerHTML = `
    <span class="section-kicker">PRO SMART INDEX</span>
    <h2>“${tag}”关联知识图谱</h2>
    <div class="member-index-summary"><div><strong>${relatedQuestions.length}</strong><span>关联真题</span></div><div><strong>${relatedMaterials.length}</strong><span>典例素材</span></div><div><strong>1</strong><span>答题模板</span></div></div>
    <article class="member-answer-angle"><span>推荐答题路径</span><strong>${angle}</strong></article>
    <div class="member-related-section"><h3>关联真题</h3>${relatedQuestions.map(q => `<button data-member-question="${q.id}"><span>${q.year} · ${q.region}</span><strong>${q.title}</strong><small>${q.type} · ${q.tags.slice(0,3).join(' / ')}</small></button>`).join('') || '<p>题库正在补充该标签的专项题。</p>'}</div>
    <div class="member-related-section"><h3>可用典例</h3>${relatedMaterials.map(m => `<button data-member-material="${m.id}"><span>${m.type}</span><strong>${m.title}</strong><small>${m.use}</small></button>`).join('') || '<p>素材库正在补充该标签的典例。</p>'}</div>
    <div class="member-index-actions"><button data-member-filter="questions" data-member-tag="${tag}">查看全部真题</button><button data-member-filter="materials" data-member-tag="${tag}">查看全部素材</button><button data-member-practice>用此考点AI训练</button></div>`;
  openSheet('member-index-sheet');
  document.querySelectorAll('[data-member-question]').forEach(button => button.addEventListener('click', () => showQuestion(Number(button.dataset.memberQuestion))));
  document.querySelectorAll('[data-member-material]').forEach(button => button.addEventListener('click', () => showMaterial(Number(button.dataset.memberMaterial))));
  document.querySelectorAll('[data-member-filter]').forEach(button => button.addEventListener('click', () => {
    closeSheets();
    navigate('library');
    state.libraryView = button.dataset.memberFilter;
    state.search = button.dataset.memberTag;
    document.querySelectorAll('[data-library-view]').forEach(tab => tab.classList.toggle('active', tab.dataset.libraryView === state.libraryView));
    document.querySelector('#material-search').value = state.search;
    renderLibrary();
  }));
  document.querySelector('[data-member-practice]').addEventListener('click', () => openSheet('ai-sheet'));
}

bindSmartTags = function(root=document) {
  root.querySelectorAll('[data-smart-tag]').forEach(button => button.addEventListener('click', event => {
    event.stopPropagation();
    showMemberIndex(button.dataset.smartTag);
  }));
};

showQuestion = function(id) {
  const q = questions.find(item => item.id === id);
  if (!q) return;
  const related = materials.filter(item => item.tags.some(tag => q.tags.includes(tag))).slice(0, 3);
  document.querySelector('#material-detail').innerHTML = `<div class="material-detail member-question-detail"><span class="material-detail-type">${q.year} · ${q.region} · ${q.type}</span><h2 class="material-detail-title">${q.title}</h2><p class="material-detail-summary">${q.summary}</p><div class="related-tags">${q.tags.map(tag => `<button data-smart-tag="${tag}"># ${tag} ✦</button>`).join('')}</div><div class="detail-divider"></div><h3>PRO审题索引</h3><div class="member-question-outline"><p><b>核心矛盾</b><span>公共价值、现实堵点与执行边界之间的平衡</span></p><p><b>建议结构</b><span>${memberAngles[q.tags[0]] || '表态定位 → 原因影响 → 对策闭环 → 价值升华'}</span></p><p><b>高分提醒</b><span>避免只有口号，对策补充执行主体、具体动作和反馈机制。</span></p></div><h3>关联典例</h3><div class="member-inline-materials">${related.map(item => `<button data-member-material="${item.id}"><strong>${item.title}</strong><small>${item.tags.slice(0,3).join(' / ')}</small></button>`).join('')}</div><button class="primary-button full" id="question-practice-now">用这道题开始AI训练</button></div>`;
  openSheet('material-sheet');
  bindSmartTags(document.querySelector('#material-detail'));
  document.querySelectorAll('#material-detail [data-member-material]').forEach(button => button.addEventListener('click', () => showMaterial(Number(button.dataset.memberMaterial))));
  document.querySelector('#question-practice-now').addEventListener('click', () => openSheet('ai-sheet'));
};

showMaterial = function(id) {
  const item = materials.find(material => material.id === id);
  if (!item) return;
  const related = questions.filter(q => q.tags.some(tag => item.tags.includes(tag))).slice(0, 3);
  document.querySelector('#material-detail').innerHTML = `<div class="material-detail member-material-detail"><span class="material-detail-type">${item.type} · PRO完整素材</span><h2 class="material-detail-title">${item.title}</h2><p class="material-detail-summary">${item.summary}</p><div class="related-tags">${item.tags.map(tag => `<button data-smart-tag="${tag}"># ${tag} ✦</button>`).join('')}</div><div class="detail-divider"></div><h3>案例内容</h3><p>${item.body}</p><h3>答题怎么用</h3><p>${item.use}</p><div class="member-material-formula"><span>一句话转化</span><strong>这个案例说明，个人善意和专业担当只有通过组织协同与制度保障，才能形成持续的公共价值。</strong></div><h3>可以练这些题</h3><div class="member-inline-materials">${related.map(q => `<button data-member-question="${q.id}"><strong>${q.title}</strong><small>${q.year} · ${q.region} · ${q.type}</small></button>`).join('')}</div><div class="member-index-actions"><button data-demo-action="已收藏到PRO素材库">收藏素材</button><button data-demo-action="已加入现象分析模板">加入我的模板</button><button data-smart-tag="${item.tags[0]}">继续智能索引</button></div></div>`;
  openSheet('material-sheet');
  bindSmartTags(document.querySelector('#material-detail'));
  bindDemoActions(document.querySelector('#material-detail'));
  document.querySelectorAll('#material-detail [data-member-question]').forEach(button => button.addEventListener('click', () => showQuestion(Number(button.dataset.memberQuestion))));
};

insertMemberInterface();
insertMemberIndex();
bindDemoActions();
renderLibrary();
setTimeout(() => toast('欢迎进入面试练场 PRO，全功能权限已开启'), 350);
